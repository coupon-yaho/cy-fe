# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.vite-test/
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# 잠금 파일은 커밋하지 않습니다 (AGENTS.md §5).
# 한 번 들어가면 리뷰마다 7,000 줄이 따라다니고, 네이티브 바이너리 의존성이
# 사람마다 달라 되레 설치가 깨집니다.
package-lock.json

# 로컬 환경변수. `.env.local` 은 위 `*.local` 이 이미 막지만 `.env` 는 안 막힌다 —
# 저장소가 공개라 배치 관리 토큰 같은 값이 여기 들어가면 그대로 노출된다.
.env
.env.*
!.env.example
